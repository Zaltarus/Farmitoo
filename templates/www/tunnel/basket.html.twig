{% extends 'www/base.html.twig' %}

{% block content %}
    <div class="page-content page-tunnel page-tunnel-basket">
        <div class="title title--medium title--primary">
            Mon panier
        </div>
        <div class="tunnel-detail basket-container-products">
            {% include 'www/components/tunnel/basket-list-product.html.twig' with {'order': order} %}
        </div>
        {% include 'www/components/tunnel/tunnel-total.html.twig' with {'order': order} %}
    </div>
{% endblock %}